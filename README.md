# Pixel Code Test Next.js Template — Tailwind v4 + NextAuth + Prisma

## Quick start
```bash
npm i
cp .env.example .env
# set NEXTAUTH_SECRET
npx prisma generate
npx prisma migrate dev --name init
npm run seed
npm run dev
```
